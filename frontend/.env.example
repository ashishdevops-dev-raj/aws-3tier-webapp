# Backend API endpoint - baked into the React build at compile time
# Local dev:  http://localhost:5000/api
# Production: /api    (NGINX proxies /api -> backend EC2)
REACT_APP_API_URL=http://localhost:5000/api
